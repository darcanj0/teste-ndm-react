import { Box } from "@mui/material";

export const PlayersSection = () => {
  return (
    <Box
      sx={{
        background: "red",
        width: "50px",
        height: "50px",
      }}
    ></Box>
  );
};
